{% extends 'base.html.twig' %}

{% block title %}Resolution{% endblock %}

{% block body %}
    <h1>Resolution</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ resolution.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ resolution.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('resolution_index') }}">back to list</a>

    <a href="{{ path('resolution_edit', {'id': resolution.id}) }}">edit</a>

    {{ include('resolution/_delete_form.html.twig') }}
{% endblock %}
